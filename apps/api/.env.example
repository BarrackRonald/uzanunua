# See the root /.env.example for the full documented list. This file
# exists so `apps/api` can be run standalone; copy the relevant values
# from root when setting up.
DATABASE_URL="postgresql://uzanunua:uzanunua_dev@localhost:5432/uzanunua?schema=public"
REDIS_URL="redis://localhost:6379"
API_PORT=3001
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
